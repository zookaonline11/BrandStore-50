# 📥 تحميل مشروع Brand Store

## ✅ المشروع جاهز للتحميل

المشروع متكامل 100% وجاهز للاستخدام الفوري. تم تنظيم جميع الملفات في مجلدات app و components بشكل صحيح.

---

## 📥 خطوات التحميل

### الطريقة 1: التحميل المباشر من v0
1. اضغط على **الزر (•••)** في الزاوية العلوية اليمين
2. اختر **"Download ZIP"**
3. سيتم تحميل المشروع كاملاً بصيغة ZIP

### الطريقة 2: استخدام Git
```bash
git clone https://github.com/[your-repo]/brand-store.git
cd brand-store
npm install
npm run dev
```

---

## 📂 تحقق من البنية بعد التحميل

بعد فك ملف ZIP، تأكد من وجود هذه المجلدات:

```
brand-store/
├── app/                    ✅ Next.js routes
├── components/             ✅ React components
├── lib/                    ✅ helper functions
├── hooks/                  ✅ custom hooks
├── public/                 ✅ static files
├── scripts/                ✅ database scripts
├── package.json            ✅
├── next.config.mjs         ✅
├── tailwind.config.ts      ✅
├── tsconfig.json           ✅
└── README.md               ✅
```

---

## 🚀 البدء الفوري (بعد التحميل)

### 1. تثبيت المكتبات
```bash
npm install
# أو
yarn install
# أو
pnpm install
```

### 2. إعداد ملف البيئة
أنسخ `.env.example` وسمّه `.env.local`:

```bash
cp .env.example .env.local
```

ثم أدخل متغيراتك:
```env
NEXT_PUBLIC_SUPABASE_URL=your_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key
SUPABASE_SERVICE_ROLE_KEY=your_key
RESEND_API_KEY=your_key
```

### 3. تشغيل المشروع
```bash
npm run dev
```

انتقل إلى: **http://localhost:3000**

---

## 🔑 حسابات التجريب

**الإدارة:**
- البريد: `shahermagdee@gmail.com`
- كلمة المرور (شاهر): `#Agehn444`
- كلمة المرور (مصطفي): `@158Afbsh`

**العميل:**
- سجل حساب جديد من `/auth/register-user`

---

## 📋 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `app/layout.tsx` | التخطيط الرئيسي |
| `app/page.tsx` | الصفحة الرئيسية |
| `app/globals.css` | الأنماط العامة |
| `package.json` | المكتبات والتبعيات |
| `.env.example` | متغيرات البيئة |
| `scripts/*.sql` | سكريبتات قاعدة البيانات |

---

## ✨ الميزات المتضمنة

✅ نظام مصادقة كامل (Admin + User)
✅ واجهة إدارة متقدمة
✅ واجهة عميل احترافية
✅ خدمات مالية (شحن، محافظ، فيزا)
✅ نظام إشعارات
✅ بوت دعم ذكي
✅ تصميم داكن مع أزرق
✅ متجاوب مع الموبايل

---

## 🆘 حل المشاكل الشائعة

### المشكلة: "Cannot find module..."
```bash
# الحل:
rm -rf node_modules
npm install
```

### المشكلة: "Port 3000 is already in use"
```bash
# الحل: استخدم port مختلف
npm run dev -- -p 3001
```

### المشكلة: "Supabase connection failed"
- تحقق من متغيرات البيئة في `.env.local`
- تأكد من أن Supabase project نشط

---

## 📞 الدعم

لمزيد من المعلومات:
- اقرأ `README.md` في الجذر
- اقرأ `PROJECT_STRUCTURE.md` للبنية
- اقرأ `FEATURES.md` للميزات الكاملة

---

## ✅ المشروع جاهز للإنتاج!

كل شيء منظم وجاهز للاستخدام الفوري. استمتع ببناء تطبيقك! 🚀
